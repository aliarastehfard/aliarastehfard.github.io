# aliarastehfard.github.io

### Local Testing
To test the site locally, run:

```bash
bundle exec jekyll serve
```

Before running the above command, ensure you have the necessary dependencies installed by running:

```bash
brew install ruby
gem install bundler
bundle install
```